import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.Locale;

//Se tiene una matriz MatLetras de MAXFILxMAXCOL caracteres. Esta matriz esta inicializada,sin espacios en blanco en ningun orden.
//Se pide que realice el diagrama de estructura y el codigo pascal para la siguiente solucion:
//Por cada fila, se eliminen las secuencias donde la misma letra se repita mas de una vez de manera consecutiva. La secuencia ser reemplazada por solo una de las letras que se repiten ,y el resto de los elemento se debe completar con espacios en blanco,y los espacios en blanco deben quedar siempre al inicio de la fila.
//Ordenar ascendentemente la matriz por filas deacuerdo a la cantidad de espacios en blancos que tenga cada una.

//Realize el DE, la declaracion de tipos, variables y el programa principal.
//Para aprobar es fundamental realizar una buena modularizacion de la solucion. No se pueden usar estructuras auxiliares.

public class Final2012Prog1 {
	final static int MAXFIL=5;
	final static int MAXCOL=5;	
	public static void main(String[] args) {
		char MatLetras[][]= {{2,2,3,4,1},{'8','3','1','1','2'},{'3','1','S','1','2'},{'T','S','Q','R','1'},{'X','Y','W','W','1'}};
		
	}
	
}
